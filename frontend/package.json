{
  "name": "thierry-wine-shit",
  "version": "1.0.0",
  "description": "Thierry's Wine Voyage",
  "proxy": "http://localhost:8000",
  "dependencies": {
    "@popperjs/core": "2.11.8",
    "axios": "^0.21.1",
    "bootstrap": "5.3.2",
    "jquery": "3.7.1",
    "react": "16.8.6",
    "react-dom": "16.8.6",
    "react-scripts": "3.0.1",
    "supports-color": "^9.4.0"
  },
  "devDependencies": {
    "typescript": "3.3.3"
  },
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "relocate": "react-scripts --openssl-legacy-provider build && rm -rf ../backend/build && mv -f build ../backend"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
